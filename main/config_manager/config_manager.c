#include "config_manager.h"

#include "esp_log.h"

static const char *TAG = "config_manager";

esp_err_t config_manager_init(void)
{
    ESP_LOGI(TAG, "Config manager stub initialized");
    return ESP_OK;
}
